export const environment = {
  production: true,
  firebase: {
    // Production Firebase config should be provided via CI/CD
    // or injected at runtime
  },
  apiUrl: 'https://streamtrack-gr75.onrender.com/api'
};
