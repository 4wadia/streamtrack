export const environment = {
  production: false,
  firebase: {
    // You'll need to fill these in your local environment.ts
    // or through another mechanism
  },
  apiUrl: 'http://localhost:3000/api'
};
